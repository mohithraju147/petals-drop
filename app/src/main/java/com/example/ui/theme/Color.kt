package com.example.ui.theme

import androidx.compose.ui.graphics.Color

// Sleek Interface Theme colors (extracted from the stunning sage, pink & slate palette)
val PetalPrimary = Color(0xFF2E7D32)       // Foliage Green (Premium Accent)
val PetalSecondary = Color(0xFFD7E8CD)     // Light Sage Green (Secondary highlights & selected states)
val PetalTertiary = Color(0xFFFCE4EC)      // Soft Blossom Pink
val PetalBackground = Color(0xFFFDFCFB)    // Clean Warm Paper White background
val PetalSurface = Color(0xFFF3F9F1)       // Soft Sage-grey header and navigation background
val PetalCard = Color(0xFFFFFFFF)          // Crisp solid white card body
val PetalTextDark = Color(0xFF1B1C17)      // Deep charcoal-olive for high-contrast typography

// Sleek Theme Specific custom colors
val SleekMutedText = Color(0xFF44483D)     // Muted olive secondary text
val SleekOutline = Color(0xFFE1E3D3)       // Soft sage border line
val SleekDarkCardBg = Color(0xFF1B1C17)    // Solid dark-slate backdrops
val SleekDarkCardText = Color(0xFFE3E3DC)  // Soft warm white text for dark cards
val SleekGreenBorder = Color(0xFFC5D9B8)   // Border for sage cards
val SleekPinkBorder = Color(0xFFF8BBD0)    // Border for pink cards
val SleekLanguageBtn = Color(0xFFE1E3D3)   // Soft grey-green language selector button
val SleekDarkPinkText = Color(0xFF3D2E32)  // Dark contrast text for pink highlights

// Elderly High Contrast colors (Solid Black & White)
val HighContrastPrimary = Color(0xFF2E7D32)    // Keep accessible Green as primary
val HighContrastTextDark = Color(0xFF000000)   // Ink Black
val HighContrastBg = Color(0xFFFFFFFF)         // Paper White
val HighContrastSurface = Color(0xFFF0F0F0)    // High Contrast Light Grey
val HighContrastCard = Color(0xFFFFFFFF)
